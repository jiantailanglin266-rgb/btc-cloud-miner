# OPERATIONS.md — 運用手順書

---

## 1. 日常運用

### 毎日

- [ ] `/admin` の「対応が必要な出金」を確認し、承認・却下する（SLA: 24時間以内）
- [ ] `FLAGGED`（異常検知）の出金は、検知理由・ユーザーの履歴・IP を確認してから判断する
- [ ] `/admin/health` で全依存が `ONLINE` であることを確認
- [ ] KYC 審査待ち（`PENDING`）を処理する

### 毎週

- [ ] `/admin/ai` の劣化予測・効率低下を確認し、必要ならプロバイダーへメンテナンスを依頼
- [ ] 監査ログの異常（深夜の管理操作・大量エクスポート）を確認
- [ ] 実 payout と Revenue Engine 推定値の乖離を確認（±5% を超えたら係数を調整）
- [ ] 依存パッケージの脆弱性（Dependabot / `npm audit`）

### 毎月

- [ ] バックアップからのリストア訓練（四半期に1回は staging へ実際に復元）
- [ ] 元帳の整合性検査（`verifyInvariants`。違反があれば即出金停止）
- [ ] レート制限・上限値の見直し
- [ ] プロバイダー SLA の実測レポート（稼働率が保証を下回っていれば補償請求）

---

## 2. 障害対応ランブック

### R1. プロバイダー API が停止した

**症状**: `/admin/health` で `OFFLINE`、ダッシュボードに DEGRADED バッジ

1. circuit breaker が自動で遮断し、最終値を表示し続けている（ユーザー影響は限定的）
2. プロバイダーのステータスページ・サポートへ確認
3. 長引く場合は `/admin/incidents` から障害情報を作成（ユーザーへ表示される）
4. **採掘自体が止まっているか**をプールの Web ダッシュボードで確認（API 停止 ≠ 採掘停止）
5. 復旧後、breaker は自動で HALF_OPEN → CLOSED に復帰する。急ぐ場合は手動同期を実行

### R2. 出金が滞留している

**症状**: `APPROVED` のまま `BROADCASTED` に進まない

1. `/admin/health` でウォレットプロバイダーの疎通を確認
2. カストディ側のステータス・残高（ホットウォレット枯渇）を確認
3. カストディ停止中でも申請・承認は継続できる。送金は復旧後に再実行される（冪等キーで二重送金は起きない）
4. ユーザーへは「送金は一時停止中」のインシデントを掲示

### R3. 不正アクセスが疑われる

1. **まず出金を全停止**: `FEATURE_WITHDRAWAL_ENABLED=false` にして再デプロイ（キルスイッチ）
2. 該当アカウントを `SUSPENDED` にする（セッションは即時無効化される）
3. 監査ログで操作履歴を追う（`/admin/audit`）
4. 影響範囲の確定まで出金は再開しない
5. 事後にインシデントレポートを作成（SECURITY.md §12）

### R4. 残高の不整合が検出された

**これは最重要インシデント。**

1. 即時に出金を全停止（R3-1 と同じ）
2. 元帳（`ledger_entries`）は追記専用なので、全履歴から残高を再導出できる
3. `verifyInvariants` の違反内容を確認（負残高 / 冪等キー重複）
4. 原因を特定するまで手動調整（`ADJUSTMENT`）を行わない
5. 調整が必要な場合は必ず `ADJUSTMENT` エントリとして記録し、監査ログに理由を残す

### R5. DB が停止した

1. Multi-AZ ならフェイルオーバーを待つ（通常 1〜2 分）
2. アプリはインメモリにフォールバックして起動するが、**この状態でユーザーに使わせない**
   （新規データが DB に入らない）。メンテナンスページを出す
3. 復旧後、`/api/health/ready` で `"store":"prisma"` を確認

### R6. シークレットをコミットしてしまった

1. **まず失効**（漏れた鍵・トークンを無効化する。履歴の削除より先）
2. 新しい値を発行し、Secrets Manager を更新
3. `git filter-repo` 等で履歴から削除し、force push（共有リポジトリでは影響者に通知）
4. GitHub の Secret Scanning アラートを確認

---

## 3. 定期ジョブ（本番で cron / スケジューラに登録するもの）

| ジョブ | 間隔 | 内容 |
|---|---|---|
| ワーカー統計同期 | 5分 | `persistSnapshots()`（管理画面の手動同期と同じ処理） |
| 時系列ロールアップ | 1時間 | 5分足 → 1時間足 → 日足 |
| 古いパーティション削除 | 日次 | 30日超の worker_snapshots |
| 日次報酬の確定 | 日次 | プールの実 payout を取り込み、earnings + 元帳へ記帳 |
| 出金確認数の更新 | 5分 | `refreshConfirmations()` |
| 元帳整合性検査 | 日次 | 全アカウントの `verifyInvariants` |
| セッション掃除 | 日次 | 期限切れセッションの削除 |
| バックアップ検証 | 週次 | 最新バックアップのリストアテスト |

MVP ではこれらをインプロセス（管理画面から手動）で実行できる。
商用では BullMQ + 専用ワーカーに載せ替える（docs/開発ロードマップ.md §6）。

---

## 4. 監視アラート（推奨閾値）

| メトリクス | 警告 | 重大 |
|---|---|---|
| API P95 レイテンシ | > 500ms | > 2s |
| 5xx 率 | > 0.5% | > 2% |
| 承認待ち出金の滞留 | > 24h | > 72h |
| プロバイダー OFFLINE | 1社 | 全社 |
| BTC データ stale | > 10分 | > 60分 |
| 元帳整合性違反 | – | 1件でも重大 |
| DB 接続エラー | 1回 | 継続 |
| ログイン失敗急増 | 20回/5分/IP | – |

## 5. データ保持・プライバシー運用

- 個人情報の削除請求: ユーザーを論理削除（`deletedAt`）→ PII を匿名化。
  取引記録・監査ログは法定保存期間（7年）まで保持し、その旨を本人に説明する
- CSV エクスポートは監査ログに記録される。不審な大量エクスポートはアラート対象
- バックアップにも個人情報が含まれる。保持期間・アクセス権を本体と同等に管理する
